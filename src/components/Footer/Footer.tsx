import React from "react";

import "./Footer.sass";

const Footer = () => {
    const classPrefix = "_component_footer_";

    return (
        <div className={`${classPrefix}wrapper`}>
            footer here
        </div>
    );
};
    
export default Footer;